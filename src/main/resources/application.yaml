spring:
  application:
    name: spring-ai-rag
  ai:
    ollama:
      chat:
        options:
          model: qwen2.5
      embedding:
        options:
          model: nomic-embed-text
sfg:
  aiapp:
    vectorStorePath: store/vectorstore.json
    documentsToLoad:
      - classpath:/movies500.csv